# 1. snow_flake算法
> 每秒上万条消息，每条消息都必须分配一条唯一的id，这些id还需要一些大致的顺序(方便客户端排序)，并且在分布式系统中不同机器产生的id必须不同。

由时间戳+机器id标识+序列号组成：
1. 时间戳总共41位，毫秒作为单位，每毫秒内序列号自增，跨毫秒序列号置零
2. 时间id标识总共10位，又可以分解为机器标识+进程标识，唯一标识即可，可使用配置完成
3. 序列号总共12位，如果在多线程中使用，可使用atomic来实现自增

一毫秒支持4096个序列号
